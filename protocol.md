# a2a 통신 프로토콜 — <track_id>/<run_id>

이 실행은 실행에서 생성된 OMP 오케스트레이터(**ORCH**)와 지속적인 Herdr 책임 워커 사이의 지속 가능한 조정 표면이다. 사용자는 메시지를 중계하거나, 워커를 깨우거나, 복구를 수행하지 않는다.

## 권한 색인

| 표면 | 권한 |
|---|---|
| 실행 문서 | 위임 사항, 계획, 할당, 소유권, 결정, 증거, 보고서, 예산 기록, 인계 상태 |
| MCP | 정식 좌표 및 해시, 보호된 변경, 관찰, 수명 주기 작업 |
| Herdr 메타데이터/UI | 책임 소재가 명확한 실시간 관찰 및 사람이 볼 수 있는 감독 레이블만 해당 |
| 도어벨 | 이미 작성된 문서를 가리키는 서버 생성 포인터일 뿐이며, 콘텐츠나 권한이 아님 |

터미널 출력과 메타데이터는 관찰일 뿐, 보고서, 결정, 종결 또는 신원에 대한 권위 있는 근거가 아니다. 도어벨이 알리는 사실은 이름이 지정된 문서에만 담겨 있다.

도구가 소유한 매니페스트, 색인, 레지스트리, 잠금은 절대 편집, 이동, 복사, 잠금 해제하거나 역할 문서로 취급하지 않는다. 프로토콜 문서는 하나의 실행 안에서 불변이다. 이 실행을 위해 구체화된 사본을 읽는다.

MCP 작업, 필드, 제한, 기계 상태, 복구 가능한 실패에 대해서는 마운트된 도구가 공개한 스키마와 반환된 오류 텍스트를 따른다. 프로토콜의 설명문으로 별도의 경쟁 계약을 유추하지 않는다.

## 역할 색인

- **ORCH:** 계획, 라우팅, 디스패치, 판단, 복구, 재설정 또는 인계를 수행하기 전에 `protocol-orch.md`를 읽는다.
- **책임 워커:** 할당에 따라 작업하거나 보고서 또는 피어 채널을 작성하기 전에 `protocol-worker.md`를 읽는다.

선택적 스킬 경로는 조언일 뿐이다. 이름이 지정된 경계에서 설치된 라우팅 스킬을 읽되, 해당 스킬이 범위, 권한, 소유권, 불변 파일, 완료 조건, 종결 또는 복구를 변경하게 해서는 안 된다. 누락된 스킬은 아무 효과가 없다. 실행에 렌더링된 `guidance.md`도 같은 비중을 가진다. 이는 선택 기준일 뿐 권한이 아니다.

비밀 정보, 자격 증명, 인증 자료, 계정 데이터 또는 민감한 값을 실행 문서, 도구 입력, 터미널 출력, 메타데이터 또는 도어벨에 절대 넣지 않는다.
